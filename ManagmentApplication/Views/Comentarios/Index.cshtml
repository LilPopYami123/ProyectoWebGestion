@model IEnumerable<ManagmentApplication.Models.Comentario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comentario1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTareaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Comentario1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTareaNavigation.IdTarea)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdComentario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdComentario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdComentario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
